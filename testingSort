# Finding Date Range

import datetime

class Node:
    def __init__(self, data):
        self.data = data
        self.next = None

    def __iter__(self):
        currentNode = self
        while currentNode:
            yield currentNode
            currentNode = currentNode.next

a = Node(datetime.date(2021,1,1))
b = Node(datetime.date(2021,1,3))
c = Node(datetime.date(2021,1,4))
d = Node(datetime.date(2021,2,2))
e = Node(datetime.date(2021,12,2))


a.next = b
b.next = c
c.next = d
d.next = e


def binarySearch(start, end):
    head = Node(None)
    endNode = head
    startFound = False
    for i in a:
        if i.data < start and startFound== False:
            head = i.next
            startFound = True
        if i.data > end:  
            break

        endNode = i

    endNode.next = None
    
    return head

for i in binarySearch(datetime.date(2021,1,3), datetime.date(2021,1,4)):
    print(i.data)